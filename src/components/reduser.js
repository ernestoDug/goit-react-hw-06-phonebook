import begincontact from 'redux/unitialAtate';
import { createAction, createReducer } from '@reduxjs/toolkit'




export const contatsReducer = createReducer([], {});




export const filterReducer = createReducer('', {});